import { render } from '@testing-library/react';

import MicrosurveyEditor from './MicrosurveyEditor';

describe('MicrosurveyEditor', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<MicrosurveyEditor />);
    // expect(baseElement).toBeTruthy();
  });
});
