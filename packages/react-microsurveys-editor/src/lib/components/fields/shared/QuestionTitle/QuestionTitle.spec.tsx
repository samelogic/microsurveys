import { render } from '@testing-library/react';

import QuestionTitle from './QuestionTitle';

describe('QuestionTitle', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<QuestionTitle />);
    // expect(baseElement).toBeTruthy();
  });
});
