import { render } from '@testing-library/react';

import MoreActionsFieldButton from './MoreActionsFieldButton';

describe('MoreActionsFieldButton', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<MoreActionsFieldButton />);
    // expect(baseElement).toBeTruthy();
  });
});
