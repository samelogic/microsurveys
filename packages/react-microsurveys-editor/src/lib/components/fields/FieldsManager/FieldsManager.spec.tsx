import { render } from '@testing-library/react';

import FieldsManager from './FieldsManager';

describe('FieldsManager', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<FieldsManager />);
    // expect(baseElement).toBeTruthy();
  });
});
