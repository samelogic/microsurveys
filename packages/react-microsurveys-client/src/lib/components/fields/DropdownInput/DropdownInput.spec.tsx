import { render } from '@testing-library/react';

import DropdownInput from './DropdownInput';

describe('DropdownInput', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<DropdownInput />);
    // expect(baseElement).toBeTruthy();
  });
});
